import 'package:flutter/cupertino.dart';

class next_word extends StatelessWidget {
  const next_word({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("hello")));
  }
}
